{
  "name": "webgl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist && parcel index.html",
    "build": "rm -rf dist && parcel build index.html --experimental-scope-hoisting",
    "build:prod": "cross-env NODE_ENV=production parcel build index.html --no-source-maps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "glslify": "^7.1.1",
    "regl": "^2.1.0"
  },
  "devDependencies": {
    "@babel/core": "^7.23.9",
    "cross-env": "^7.0.3",
    "parcel-bundler": "^1.12.5",
    "parcel-plugin-glsl": "^0.1.0",
    "parcel-plugin-inline-source": "^1.0.0"
  }
}
